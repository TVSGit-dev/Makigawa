# Clé API intervals.icu — NE JAMAIS committer la vraie valeur.
# Cette clé vit côté serveur uniquement (fonction serverless ou secret de
# dépôt) : elle ne doit jamais être exposée au front, donc jamais préfixée
# VITE_, qui l'inclurait dans le bundle envoyé au navigateur.
INTERVALS_ICU_API_KEY=
INTERVALS_ICU_ATHLETE_ID=
